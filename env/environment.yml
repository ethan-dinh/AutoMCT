name: microct-analysis
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  # Core scientific stack
  - numpy
  - scipy
  - scikit-image
  - tifffile
  - networkx
  - plotly
  - tqdm
  - pillow
  - matplotlib
  # CLI / TUI utilities
  - rich
  # Visualization
  - napari
  - pyqt6
  # Build tools
  - pip
  - pip:
      # NRRD file I/O
      - pynrrd
      # DICOM reading (used by tools/convert_DICOM_NRRD.py)
      - pydicom>=2.4
      # Deep learning (ML-Segmentation module)
      # For GPU support, install PyTorch separately — see env/README.md
      - torch>=2.0
      - monai>=1.3
